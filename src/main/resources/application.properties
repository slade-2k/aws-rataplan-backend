###RATAPLAN FALLBACK CONFIG###

###Rataplan run-mode
# true sendet mails (IN ALLEN TESTS!!!)
RATAPLAN.PROD=false

###Spring profile
spring.profiles.active=dev,prod

###Rataplan Hibernate-configuration
hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
hibernate.show_sql=true
hibernate.hbm2ddl.auto=validate
entitymanager.packagesToScan=de.iks.rataplan

###Rataplan E-Mail-configuration
mail.smtp.port=25
mail.smtp.host=0.0.0.0
mail.from=information@rataplan.de

###Rataplan Database-configuration
JDBC_DATABASE_DRIVER=org.postgresql.Driver
JDBC_DATABASE_URL=jdbc:postgresql://localhost:5432/rataplandb
JDBC_DATABASE_USERNAME=postgres
JDBC_DATABASE_PASSWORD=

###Mail configuration
SENDGRID_API_KEY=API_KEY-like:SG.lalala.lolo

### Flyway
flyway.locations=migration.prod

###URLs of own services
rataplan.backend.domain=localhost

rataplan.auth.url=http://localhost:8081/v1

rataplan.frontend.url=http://localhost:4200

###own server port
server.port=8080